# pnpm settings
enable-pre-post-scripts=true
# Match the lockfile's settings.autoInstallPeers so `--frozen-lockfile` works in
# CI (which has no user ~/.npmrc; pnpm would otherwise default this to true).
auto-install-peers=false
