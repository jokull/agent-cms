- Try D1-native batching for per-table StructuredText block fetches, if the Worker SQL layer can expose it cleanly. Effect-level parallel query scheduling regressed, but a single driver-level batch may still reduce round-trip/scheduling overhead without increasing contention.
- Explore driver-level prepared-statement reuse or lower-overhead raw D1 access specifically for the hot StructuredText preview fetch loop in `src/services/structured-text-service.ts`, since most object-level and resolver-level micro-optimizations have regressed.
