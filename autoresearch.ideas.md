- Explore whether request-scoped caching of resolved block model schemas can be widened beyond a single materializeContext without keeping too much long-lived state, since broad loader-level sharing regressed but the per-context schema cache is clearly valuable.
- Look for cheap hot-path wins inside candidate block-model selection and nested request assembly in `src/services/structured-text-service.ts` now that the obvious D1-adjacent metadata reads have all regressed.
