{
  "name": "test-blog-cms",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply test-blog-cms-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply test-blog-cms-db --remote"
  },
  "dependencies": {
    "agent-cms": "file:../../..",
    "wrangler": "^4.73.0"
  }
}
